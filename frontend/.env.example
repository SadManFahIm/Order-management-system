# ── Frontend environment ─────────────────────────────────────
# Copy to `.env` if you need a custom API URL.
# In development the Vite proxy forwards `/api` to the backend automatically.
# In production nginx proxies `/api` to the backend service.

# VITE_API_URL=/api
